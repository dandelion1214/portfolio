# AI 协作指南 - 王楚杰个人作品集项目

> 本文档面向接手此项目的 AI Agent，帮助快速理解项目背景、目标和协作方式

## 📋 项目概述

### 项目目标
为王楚杰（AI内容运营专家）创建一个专业的个人作品集网站，用于求职展示。

### 核心定位
- **目标受众**: 招聘方、面试官、潜在雇主
- **展示重点**: AI应用能力 + 内容运营成果 + 数据驱动思维
- **技术选型**: 纯前端（HTML/CSS/JS），无需后端，易于部署和分享

### 设计理念
- **Apple Design Language**: 极简、精致、克制
- **数据可视化**: 用图表展示账号增长趋势和运营成果
- **模块化展示**: 账号案例 → 系列案例 → 内容能力 → AI工具

## 🗂️ 文档阅读顺序

如果你是第一次接触这个项目，建议按以下顺序阅读文档：

### 1️⃣ 快速了解（5分钟）
- **README.md** - 项目简介、快速开始、文件结构
- **文件清单.md** - 完整的文件列表和统计

### 2️⃣ 深入理解（15分钟）
- **CLAUDE.md** - 技术约定、设计规范、历史决策（AI协作的核心文档）
- **项目文档.md** - 完整的设计系统、用户需求记录、架构设计

### 3️⃣ 实际操作（按需）
- 打开 `pages/index.html` 预览网站
- 查看各个页面的HTML代码理解实现方式
- 参考 `common.css` 了解共享样式

## 🎯 项目核心信息

### 网站结构（两级架构）

```
一级页面: index.html（主页）
├── Hero区（AI logo动画）
├── 账号分析案例（3个账号）
│   ├── GM的秘密基地 → gm_detail.html
│   ├── 史上醉 → ssz_detail.html
│   └── 超级嘎嘎 → cjgg_detail.html
├── 系列案例分析（2个系列）
│   ├── 自孵化系列 → series_self.html
│   └── 供应商系列 → series_supplier.html
├── 内容能力展示（2个能力）
│   ├── 封面展示 → cover.html
│   └── 文案展示 → copywriting.html
└── AI工具展示（3个工具）
    ├── 开头AI工具 → opening-ai.html
    ├── 自动化选题库 → topic-ai.html
    └── 热点选题灵感 → hotspot-ai.html

二级页面: 各详情页
└── 展示具体案例的完整信息
```

### 设计系统核心要素

**色彩**:
- 主色: `#ffffff` (白色)
- 强调色: `#0066cc` (蓝色)
- 文字: `#1d1d1f` (深灰黑), `#86868b` (中灰)

**字体**:
- 字体家族: `-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC"`
- 大标题: 96px, 字重700, 字间距-0.02em

**效果**:
- 毛玻璃: `backdrop-filter: saturate(180%) blur(20px)`
- 双层阴影: `0 2px 8px rgba(0,0,0,0.04), 0 8px 24px rgba(0,0,0,0.08)`
- 动画曲线: `cubic-bezier(0.32, 0.72, 0, 1)` (Apple标准)

### 技术约定

**图表配置（Chart.js）**:
```javascript
// ⚠️ 重要：数据预处理
const viewsData = data.map(item => item.views / 10000); // 转换为万

// ⚠️ 纵坐标callback：直接显示，不要再除
ticks: {
    callback: function(value) {
        return value.toFixed(0) + 'w';  // 已经是万为单位
    }
}
```

**常见错误**:
- ❌ 数据已除以10000，纵坐标又除以10000 → 显示错误
- ✅ 数据除以10000，纵坐标直接显示value

**图片尺寸规范**:
- 爆款展示封面: `width: 8%`（相对单位，不用固定像素）
- 竖版比例: `aspect-ratio: 9/16`
- 移动端: `width: 25%`

## 🔧 常见协作场景

### 场景1: 修改页面内容
1. 先读取对应的HTML文件
2. 确认当前结构和样式
3. 使用 `strReplace` 精确替换
4. 在浏览器中验证效果
5. **⚠️ 完成后必须询问**: "是否需要推送更新到GitHub？"

### 场景2: 添加新页面
1. 参考现有页面结构（如 `opening-ai.html`）
2. 复用 `common.css` 的样式
3. 保持设计风格一致（Apple Design Language）
4. 更新 `index.html` 添加入口链接
5. 更新 `CLAUDE.md` 的"已完成功能"清单
6. **⚠️ 完成后必须询问**: "是否需要推送更新到GitHub？"

### 场景3: 修改图表
1. 先读取对应的详情页HTML
2. 找到 Chart.js 配置部分
3. 确认数据是否已经除以10000
4. 修改配置，确保纵坐标不重复除
5. 验证拐点位置和颜色
6. **⚠️ 完成后必须询问**: "是否需要推送更新到GitHub？"

### 场景4: 添加/替换图片
1. 将图片放到对应的文件夹（如 `cover-images/`）
2. 使用相对路径引用（如 `cover-images/image.png`）
3. 更新HTML中的图片路径
4. 更新 `文件清单.md` 的图片统计
5. **⚠️ 完成后必须询问**: "是否需要推送更新到GitHub？"

## 🚀 GitHub 更新工作流（重要！）

### ⚠️ 强制规则
**任何对作品集的修改完成后，AI必须主动询问用户：**

> "修改已完成！是否需要推送更新到GitHub？如果需要，我会帮你：
> 1. 提交所有更改
> 2. 推送到GitHub
> 3. 整理更新内容到项目文档"

### 更新流程（三步走）

#### 第1步：提交更改
```bash
cd "/Users/chujie/kiro/个人作品集/portfolio-clean"
git add .
git commit -m "描述本次修改的内容"
git push
```

#### 第2步：验证部署
- GitHub Pages会自动重新部署（1-3分钟）
- 网站地址：https://dandelion1214.github.io/portfolio/

#### 第3步：更新项目文档
在 `CLAUDE.md` 或 `AI协作指南.md` 中记录：
- 修改日期
- 修改内容
- 涉及的文件
- 重要决策或注意事项

### 快速更新命令
用户也可以使用快速脚本：
```bash
cd "/Users/chujie/kiro/个人作品集/portfolio-clean"
./update.sh "更新说明"
```

### GitHub仓库信息
- **仓库地址**: https://github.com/dandelion1214/portfolio
- **GitHub用户名**: Dandelion1214
- **Git邮箱**: wangchujie1214@gmail.com
- **部署方式**: GitHub Pages (main分支, /docs文件夹)
- **网站地址**: https://dandelion1214.github.io/portfolio/

### 常见更新场景示例

**场景A: 修改了文案**
```
✅ 修改完成后询问：
"文案已更新！是否需要推送到GitHub让网站生效？"

用户确认后执行：
git add .
git commit -m "更新XX页面文案"
git push
```

**场景B: 添加了新图片**
```
✅ 修改完成后询问：
"新图片已添加！是否需要推送到GitHub？"

用户确认后执行：
git add .
git commit -m "添加XX页面新图片"
git push
```

**场景C: 修改了多个页面**
```
✅ 修改完成后询问：
"已完成以下修改：
1. 供应商系列页面：添加迭代详情
2. AI工具页面：删除展示icon
3. 关键词蓝色高亮

是否需要推送到GitHub？"

用户确认后执行：
git add .
git commit -m "优化供应商系列迭代详情和AI工具页面展示"
git push
```

### 📋 更新检查清单

每次推送前确认：
- [ ] 所有修改的文件都已保存
- [ ] 在本地浏览器中验证过效果
- [ ] commit信息清晰描述了修改内容
- [ ] 如有重要变更，已更新项目文档

### 🎯 为什么这很重要？

1. **保持同步**: 确保GitHub上的代码和本地一致
2. **网站更新**: 只有推送后，线上网站才会更新
3. **版本管理**: 每次修改都有记录，方便回溯
4. **协作效率**: 其他AI或开发者能看到最新状态

## ⚠️ 重要约定（红线）

### 设计红线
1. **严格遵循Apple设计语言** - 不要添加花哨的装饰
2. **保持极简主义** - 去除一切不必要的元素
3. **色彩克制** - 只用白色、蓝色、灰色
4. **动画流畅** - 使用Apple标准缓动曲线

### 技术红线
1. **纯前端实现** - 不要引入后端依赖
2. **相对路径** - 所有资源使用相对路径
3. **响应式设计** - 断点768px，移动端单列布局
4. **图表数据处理** - 数据预处理后，纵坐标不要再除

### 文档红线
1. **同步更新** - 代码变更后必须更新 `CLAUDE.md`
2. **绝对时间** - 文档中不要出现"今天"、"最近"等相对时间
3. **保持一致** - 三个详情页的结构和样式应保持一致

## 📝 协作最佳实践

### 修改前
1. ✅ 先读取相关文件，确认当前状态
2. ✅ 理解用户需求，明确修改目标
3. ✅ 检查是否有类似的实现可以参考

### 修改中
1. ✅ 使用精确的字符串替换（`strReplace`）
2. ✅ 保持代码风格一致
3. ✅ 添加必要的注释

### 修改后
1. ✅ 更新相关文档（`CLAUDE.md`、`README.md`）
2. ✅ 在浏览器中验证效果
3. ✅ 检查是否影响其他页面

## 🎓 学习资源

### 设计参考
- [Apple Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
- Apple官网的设计风格

### 技术文档
- [Chart.js Documentation](https://www.chartjs.org/docs/latest/)
- [MDN Web Docs](https://developer.mozilla.org/)

## 🤝 与用户协作

### 用户特点
- **王楚杰**: AI内容运营专家，对设计有明确要求
- **沟通风格**: 直接、具体，会提供详细的需求描述
- **关注点**: 视觉效果、数据准确性、专业性

### 常见需求类型
1. **视觉调整**: 间距、颜色、字号、布局
2. **内容更新**: 文案、图片、数据
3. **功能新增**: 新页面、新模块、新交互
4. **问题修复**: 图表显示错误、链接失效、样式问题

### 沟通建议
1. ✅ 修改前先确认理解正确
2. ✅ 提供多个方案供选择（如有必要）
3. ✅ 修改后说明具体改了什么
4. ✅ 遇到技术限制时及时说明

## 📞 项目联系人

- **项目所有者**: 王楚杰
- **邮箱**: 1034892264@qq.com
- **电话**: 18042014312

## 🔄 版本信息

- **当前版本**: v1.0
- **最后更新**: 2026-05-06
- **项目状态**: 核心功能已完成，待扩展AI工具板块

## 📌 快速命令参考

```bash
# 本地预览
open pages/index.html

# 查看文件结构
ls -la pages/

# 检查文件大小
du -sh portfolio-clean/

# 查找特定文件
find pages/ -name "*.html"
```

---

**提示**: 如果你是第一次接手这个项目，建议先花15-20分钟通读 `CLAUDE.md` 和 `项目文档.md`，这会帮助你快速建立对项目的全局理解。
